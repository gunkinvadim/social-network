const initialState = {
    friends: [
        {id: 1, name: 'Andrew'},
        {id: 2, name: 'Sergey'},
        {id: 3, name: 'Vadim'}
    ]
}


const sidebarReducer = (state = initialState, action) => {
    return state
}

export default sidebarReducer